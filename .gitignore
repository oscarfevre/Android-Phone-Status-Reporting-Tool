# Ignore Gradle project-specific cache directory
.gradle

# Ignore build outputs and APKs
build
app/build/
*/build/
*.apk
*.ap_
java_pid*.hprof
java_pid*.hprof

# Keystores and signing configs (kept local only)
*.jks
*.keystore
*.keystore.properties
signing.properties

# Firebase config (add your own google-services.json locally, do not commit)
/app/google-services.json
local.properties

# IDE files
.idea/
