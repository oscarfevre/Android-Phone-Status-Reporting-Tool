# Ignore Gradle project-specific cache directory
.gradle

# Ignore Gradle build output directory
build
java_pid*.hprof
java_pid*.hprof

# Keystores and signing configs (kept local only)
*.jks
*.keystore
*.keystore.properties
signing.properties

# Firebase config (add your own google-services.json locally, do not commit)
/app/google-services.json
